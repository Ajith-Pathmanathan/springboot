spring.application.name=phase1-core

# Logging — show DEBUG for our packages to see BPP, lifecycle, AOP in action
logging.level.com.techleadguru=INFO
logging.level.com.techleadguru.phase1.day04=DEBUG

# Day 21: Auto-configuration demo
# Set to true to activate AuditAutoConfiguration
app.audit.enabled=true
app.audit.log-level=INFO

# Show Spring Boot startup debug output (disable in prod)
# spring.main.log-startup-info=true

# AOP proxy config (needed for Day 19 fix)
# Already set via @EnableAspectJAutoProxy(exposeProxy=true) in Phase1Application
